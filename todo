first last -> floor ceil
for stack queue deque move include list to .h file
oset omap: fns and header file prototypes same order
rename the get_count fns to size (also do for other containers)
for get-esque fns, return value, ptr or const ptr?
key val params to pair_init()?

FOR ALL:
- NULL container causes crash
- empty container doesn't
make the above two consistnet
